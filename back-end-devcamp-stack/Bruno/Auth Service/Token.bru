meta {
  name: Token
  type: http
  seq: 1
}

post {
  url: {{baseurl}}:{{port_auth}}/token
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar('jwt', res.getBody());
}

settings {
  encodeUrl: true
}
